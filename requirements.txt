# --- Étape 1 : catalogue + recherche ---
pandas>=2.2
openpyxl>=3.1          # lecture des fichiers .xlsx
rapidfuzz>=3.9         # recherche floue (fuzzy)
python-dotenv>=1.0     # chargement du .env
truststore>=0.9        # correctif SSL Windows (magasin de certificats OS)

# --- Étape 2 : IA (moteur interchangeable) ---
openai>=1.40             # OpenAI (payant, fiable)
groq>=0.11               # Groq (gratuit, Llama 3.3)
google-genai>=1.0        # Gemini (alternative, si facturation activée)

# --- Étape 3 : serveur webhook WhatsApp ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27            # appels HTTP vers l'API WhatsApp
